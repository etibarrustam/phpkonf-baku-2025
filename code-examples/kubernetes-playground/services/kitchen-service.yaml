apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
  labels:
    app: kitchen-service
    tier: application
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: kitchen-service
  sessionAffinity: None
